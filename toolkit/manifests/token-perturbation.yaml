experiment_id: token-perturbation-test
author: "LLM Red-Teaming Toolkit"
description: "Test token perturbation attacks with subtle adversarial modifications"

adapters:
  - instance_id: adapter-stub
    adapter: stub
    model_id: stub-v1

generators:
  - instance_id: generator-token-perturbation
    name: token-perturbation
    config:
      seed: 42

judges:
  - instance_id: judge-heuristic
    name: heuristic

batch_size: 8
num_batches: 2
max_concurrency: 4
max_combo_concurrency: 1
pipeline_mode: "batch"
output: "logs/token-perturbation.jsonl"
